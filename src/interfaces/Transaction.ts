export interface Transaction {
    date: Date,
    sign: string,
    amount: number,
    currency: string,
    organisation: string,
    location: string,
    category: string,
    description: string,
  }
  